import { supabase } from '@/lib/supabaseClient';

export default async function handler(req, res) {
    // const { data, error } = await supabase.from('data').select('id','title');
    // res.status(200).json(data);
}